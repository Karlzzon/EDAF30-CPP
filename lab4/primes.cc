#include <string>
#include <iostream>

using namespace std;

int main()
{
	string primes(100000, 'P');
	primes.replace(0,2, "CC")
	
	int current = 0; 
	
	while(current = primes.find(current, 'P') != string::npos){
		
	}

}
